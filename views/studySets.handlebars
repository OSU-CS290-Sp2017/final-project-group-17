{{> header}}

<main class="sets-page">

  <div class="title-container">
    <h1>Study sets of {{name}}</h1>
  </div>

  <div class="photo-card-container">

    {{#each photos}}
      {{> photoCard}}
    {{/each}}

  </div>

</main>

{{> addPhotoModal}}
